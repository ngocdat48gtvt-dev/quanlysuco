# Ảnh screenshot

Ảnh app hiện nằm trong thư mục **`Ho so dang CH play/`** (`1.jpg` … `4.jpg`).

Không cần copy thêm vào đây trừ khi bạn muốn tách thư mục — khi đó sửa `assetsFolder` trong `js/config.js`.
